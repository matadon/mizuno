require 'spec_helper'

describe Mizuno do
    include HttpRequests

    pending "loads global server configuration from a file" do
    end

    pending "loads individual app configurations from files" do
    end

    pending "mounts apps under different paths" do
    end

    pending "writes server logs to a file" do
    end

    pending "allows for rotation of server logs" do
    end

    pending "starts as a daemon" do
    end

    pending "stops a running daemon" do
    end

    pending "reloads all apps on SIGHUP" do
    end

    pending "reloads an app when a trigger file is modified" do
    end

end
