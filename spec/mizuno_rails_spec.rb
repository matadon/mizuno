require 'spec_helper'

describe 'rails support' do
    pending "sets the environment for rails" do
    end

    pending "runs in development mode" do
    end

    pending "runs in production mode" do
    end

    pending "serves up static assets in production mode" do
    end
end
