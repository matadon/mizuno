#!/usr/bin/env ruby

# Make sure we're on JRuby.
raise("Rack::Handler::Jetty only runs on JRuby.") \
    unless (RUBY_PLATFORM =~ /java/)

require 'rack'
require 'rack/handler/jetty'

server = Rack::Server.new
server.options[:server] = 'jetty'
server.start
